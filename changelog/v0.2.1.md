## v0.2.1

### Fixes
- Windows: write fatal startup errors to `~/.bookunbind/crash.log` so blank-window crashes are diagnosable.
- Ensure the app root is on `sys.path` in packaged builds so `from src...` imports resolve on Windows.
