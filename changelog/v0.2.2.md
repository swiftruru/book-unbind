## v0.2.2

### Fixes
- Windows diagnostics: write heartbeat + crash traces to `~/.bookunbind/crash.log` from the very first line of `main.py`, so blank-window cases reveal whether `main.py` was reached and which import failed.
