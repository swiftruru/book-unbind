## v0.2.3

### Fixes
- Windows packaging: switch from PowerShell's `Compress-Archive` to 7-Zip so hidden files, empty directories, and the embedded Flet Python bundle (under `data/flutter_assets/app/`) survive the zip round-trip. Fixes the blank-window launch on Windows caused by a missing/broken Python app bundle.
