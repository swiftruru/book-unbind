## v0.2.5

### Fixes
- CI: drop `ChineseTraditional.isl` from the Inno Setup language list. It is not bundled with Inno Setup 6.7 on `windows-latest`, which caused the v0.2.4 Windows build to fail. Installer UI is now English-only; the app itself stays zh-TW.
